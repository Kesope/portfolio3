define([], function () {
    return {
        events: {
            selectComment: "SelectComment",
            submitComment: "SubmitComment"
            //search : "Search"
        },
        menuItems: {
            history: "Search History",
            comments: "Comments",
            posts: "Posts"
        }
    };
});
